# Only install package versions published at least 7 days ago (value is in minutes).
min-release-age=10080
save-exact=true
engine-strict=true
# Unreviewed install scripts (preinstall/install/postinstall) fail the install
# instead of running. Maintain the allowlist with `npm approve-scripts`.
strict-allow-scripts=true
